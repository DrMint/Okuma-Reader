/*  PLEASE CONFIGURE THOSE VALUES OR OKUMA WON'T WORK.
    booksURL can be on a different domain/subdomain if you prefer.
    The books are stored in a library. The library name can be whatever you want (here it's books)
*/
export function homeURL() {return        "https://domain.com/"}
export function readerURL() {return      "https://domain.com/read.html"}
export function booksURL() {return       "https://domain.com/books/"}
export function websiteName() {return    "WEBSITE TITLE"}
